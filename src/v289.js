// FitTogether V2.0.89: resume card on home after reload/app termination.
const r289=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}}
const fmt289=sec=>{sec=Math.max(0,Math.floor(sec||0));const h=Math.floor(sec/3600),m=Math.floor(sec%3600/60);return h?`${h} Std. ${m} Min.`:`${m} Min.`}
const render289=()=>{if(document.querySelector('.active-training,.rest-overlay'))return;const state=r289('ft-active-training-v288',null);const home=document.querySelector('.welcome')?.parentElement||document.querySelector('main');let card=document.querySelector('.resume-training-v289');if(!state?.exercises?.length||!home){card?.remove();return}const elapsed=Math.max(0,(Date.now()-(Number(state.startedAt)||Date.now()))/1000),i=Math.min(state.exercises.length,Math.max(1,(Number(state.index)||0)+1)),set=Math.max(1,Number(state.set)||1),sets=Math.max(1,Number(state.exercises?.[i-1]?.sets)||3),name=state.exercises?.[i-1]?.name||'Training';if(!card){card=document.createElement('section');card.className='resume-training-v289';const anchor=home.querySelector('.today,.start-strip,.profile-shortcut');anchor?.insertAdjacentElement('beforebegin',card)||home.appendChild(card)}card.innerHTML=`<div class="resume-head-v289"><div><small>TRAINING LÄUFT</small><strong>${name}</strong><span>${fmt289(elapsed)} · Übung ${i}/${state.exercises.length} · Satz ${set}/${sets}</span></div><b>FORTSETZEN</b></div><div class="resume-actions-v289"><button type="button" data-resume289>Training fortsetzen</button><button type="button" data-discard289>Verwerfen</button></div>`;card.querySelector('[data-resume289]')?.addEventListener('click',()=>{window.__FT_RESUME_289=true;window.FitTogetherStartNormalTraining?.(state.exercises)});card.querySelector('[data-discard289]')?.addEventListener('click',()=>{try{localStorage.removeItem('ft-active-training-v288');localStorage.removeItem('ft-rest-deadline-v289');localStorage.removeItem('ft-active-set-logs-v277')}catch{};card.remove()})}
let q289=false;const schedule289=()=>{if(q289)return;q289=true;requestAnimationFrame(()=>{q289=false;render289()})};if(typeof document!=='undefined'){const start=()=>{render289();new MutationObserver(m=>{if(m.some(x=>x.addedNodes.length||x.removedNodes.length))schedule289()}).observe(document.body,{childList:true,subtree:true});window.addEventListener('pageshow',schedule289);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')schedule289()})};document.body?start():document.addEventListener('DOMContentLoaded',start,{once:true})}
