const CURRENT_VERSION='V2.0.145'
let checking=false
const VERSION_SELECTOR='.brand b,.training-note,.version,.ft-auth-copy small'
const syncVisibleVersion=(root=document)=>{const nodes=[];if(root?.nodeType===1&&root.matches?.(VERSION_SELECTOR))nodes.push(root);root?.querySelectorAll?.(VERSION_SELECTOR)?.forEach(el=>nodes.push(el));for(const el of nodes){const text=el.textContent||'';if(/V2\.0\.\d+/.test(text))el.textContent=text.replace(/V2\.0\.\d+/g,CURRENT_VERSION)}}
const clearLegacyWebCache=async()=>{try{if('serviceWorker'in navigator){const regs=await navigator.serviceWorker.getRegistrations();await Promise.all(regs.map(r=>r.unregister()))}}catch{}try{if('caches'in window){const keys=await caches.keys();await Promise.all(keys.map(k=>caches.delete(k)))}}catch{}}
const forceFreshBuild=async version=>{await clearLegacyWebCache();const url=new URL(window.location.href);url.searchParams.set('ftv',version);url.searchParams.set('_',String(Date.now()));window.location.replace(url.toString())}
const checkForUpdate=async()=>{if(checking)return;checking=true;try{const response=await fetch(`/version.json?ft=${Date.now()}`,{cache:'reload',headers:{'Cache-Control':'no-cache, no-store','Pragma':'no-cache'}});if(!response.ok)return;const data=await response.json();if(data?.version&&data.version!==CURRENT_VERSION){await forceFreshBuild(data.version);return}}catch{}finally{checking=false}}
if(typeof window!=='undefined'){window.__FT_BUILD_VERSION__=CURRENT_VERSION;const start=()=>{syncVisibleVersion(document);clearLegacyWebCache();const observer=new MutationObserver(mutations=>{for(const mutation of mutations){mutation.addedNodes.forEach(node=>{if(node.nodeType===1)syncVisibleVersion(node)})}});observer.observe(document.body,{childList:true,subtree:true});const url=new URL(window.location.href);if(url.searchParams.get('ftv')===CURRENT_VERSION){url.searchParams.delete('ftv');url.searchParams.delete('_');history.replaceState(null,'',url.pathname+url.search+url.hash)}};document.body?start():document.addEventListener('DOMContentLoaded',start,{once:true});window.addEventListener('pageshow',checkForUpdate);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')checkForUpdate()});setTimeout(checkForUpdate,400)}
