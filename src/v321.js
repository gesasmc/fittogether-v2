// FitTogether V2.0.157: import a complete training plan from pasted text and map exercises to the existing library.
const MUSCLES321=['abductors','abs','adductors','biceps','calves','cardio','delts','forearms','glutes','hamstrings','lats','levator-scapulae','pectorals','quads','serratus-anterior','spine','traps','triceps','upper-back']
const API321='https://raw.githubusercontent.com/JahelCuadrado/ExerciseGymGifsDB/main/api/en/muscles/'
const read321=(k,f)=>{try{return JSON.parse(localStorage.getItem(k))??f}catch{return f}}
const write321=(k,v)=>{try{localStorage.setItem(k,JSON.stringify(v))}catch{}}
const esc321=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))
const norm321=s=>String(s||'').toLocaleLowerCase('de-DE').replace(/ä/g,'ae').replace(/ö/g,'oe').replace(/ü/g,'ue').replace(/ß/g,'ss').replace(/[^a-z0-9]+/g,' ').trim()
const pretty321=name=>String(name||'').replaceAll('-',' ').replace(/\bstationary bike\b/gi,'Fahrradergometer').replace(/\bexercise bike\b/gi,'Fahrradergometer').replace(/\bcycling\b/gi,'Radfahren').replace(/\browing machine\b/gi,'Rudergerät').replace(/\btreadmill\b/gi,'Laufband').replace(/\bbarbell\b/gi,'Langhantel').replace(/\bdumbbell\b/gi,'Kurzhantel').replace(/\bbench press\b/gi,'Bankdrücken').replace(/\bshoulder press\b/gi,'Schulterdrücken').replace(/\bbiceps curl\b/gi,'Bizeps-Curl').replace(/\btriceps\b/gi,'Trizeps').replace(/\bsquat\b/gi,'Kniebeuge').replace(/\bdeadlift\b/gi,'Kreuzheben').replace(/\blateral raise\b/gi,'Seitheben').replace(/\bpush up\b/gi,'Liegestütz').replace(/\bpull up\b/gi,'Klimmzug').replace(/\bleg press\b/gi,'Beinpresse').replace(/\bleg extension\b/gi,'Beinstrecken').replace(/\bleg curl\b/gi,'Beinbeugen').replace(/\bcalf raise\b/gi,'Wadenheben')
const isCardio321=x=>{const s=`${x?.bodyPart||''} ${x?.muscle||''} ${x?.category||''} ${x?.name||''}`.toLowerCase();return /cardio|cardiovascular|bike|bicycle|cycling|treadmill|rowing machine|rower/.test(s)}
const dayMap321={montag:'Mo',mo:'Mo',monday:'Mo',dienstag:'Di',di:'Di',tuesday:'Di',mittwoch:'Mi',mi:'Mi',wednesday:'Mi',donnerstag:'Do',do:'Do',thursday:'Do',freitag:'Fr',fr:'Fr',friday:'Fr',samstag:'Sa',sa:'Sa',saturday:'Sa',sonntag:'So',so:'So',sunday:'So'}
const fullDay321={Mo:'Montag',Di:'Dienstag',Mi:'Mittwoch',Do:'Donnerstag',Fr:'Freitag',Sa:'Samstag',So:'Sonntag'}
const alias321={
 'bankdruecken':['bench press'], 'kurzhantel bankdruecken':['dumbbell bench press'], 'langhantel bankdruecken':['barbell bench press'],
 'schraegbankdruecken':['incline bench press'], 'kurzhantel schraegbankdruecken':['incline dumbbell bench press'], 'langhantel schraegbankdruecken':['incline barbell bench press'],
 'schulterdruecken':['shoulder press'], 'seitheben':['lateral raise'], 'frontheben':['front raise'], 'latziehen':['lat pulldown','pulldown'],
 'klimmzug':['pull up'], 'klimmzuege':['pull up'], 'rudern':['row'], 'langhantelrudern':['barbell bent over row','barbell row'], 'kurzhantelrudern':['dumbbell row'],
 'kniebeuge':['squat'], 'kniebeugen':['squat'], 'kreuzheben':['deadlift'], 'rumaenisches kreuzheben':['romanian deadlift'], 'beinpresse':['leg press'],
 'beinbeugen':['leg curl'], 'beinstrecken':['leg extension'], 'wadenheben':['calf raise'], 'hip thrust':['hip thrust'], 'hueftheben':['hip thrust'],
 'brust fliegende':['chest fly','dumbbell fly'], 'fliegende':['fly'], 'trizepsdruecken':['triceps pushdown'], 'bizeps curl':['biceps curl'], 'bizepscurls':['biceps curl'],
 'liegestuetz':['push up'], 'liegestuetze':['push up'], 'dips':['dip'], 'plank':['plank'], 'fahrrad':['stationary bike','exercise bike','cycling'],
 'fahrradergometer':['stationary bike','exercise bike'], 'ergometer':['stationary bike','exercise bike'], 'radfahren':['cycling'], 'rudergeraet':['rowing machine','rower'],
 'laufband':['treadmill'], 'laufen':['treadmill','running']
}
let db321=null,dbPromise321=null
const loadDb321=()=>db321?Promise.resolve(db321):(dbPromise321||(dbPromise321=Promise.all(MUSCLES321.map(m=>fetch(`${API321}${m}.json`).then(r=>r.ok?r.json():{exercises:[]}).then(g=>(g.exercises||[]).map(x=>({...x,muscle:x.muscle||m}))).catch(()=>[]))).then(parts=>{const map=new Map();parts.flat().forEach(x=>{const k=String(x?.id||x?.name||'').toLowerCase();if(k&&!map.has(k))map.set(k,x)});db321=[...map.values()];return db321}).catch(()=>[])))
const aliasesFor321=name=>{const n=norm321(name),out=[n];Object.entries(alias321).forEach(([k,vals])=>{if(n===k||n.includes(k)||k.includes(n))vals.forEach(v=>out.push(norm321(v)))});return[...new Set(out.filter(Boolean))]}
const equipmentHint321=name=>{const n=norm321(name);if(/kurzhantel|dumbbell/.test(n))return'dumbbell';if(/langhantel|barbell/.test(n))return'barbell';if(/kabel|cable/.test(n))return'cable';if(/maschine|machine/.test(n))return'machine';if(/koerpergewicht|bodyweight/.test(n))return'body';return''}
const rank321=(raw,items)=>{const variants=aliasesFor321(raw),hint=equipmentHint321(raw);return items.map(x=>{const en=norm321(x?.name),de=norm321(pretty321(x?.name)),eq=norm321(x?.equipment);let score=0;for(const q of variants){if(!q)continue;if(en===q||de===q)score=Math.max(score,120);else if(en.includes(q)||de.includes(q)||q.includes(en)||q.includes(de))score=Math.max(score,82);const qt=q.split(' ').filter(t=>t.length>2),hay=`${en} ${de}`;const hits=qt.filter(t=>hay.includes(t)).length;if(qt.length)score=Math.max(score,Math.round(hits/qt.length*55))}if(hint){if(eq.includes(hint))score+=18;else if(hint==='machine'&&/leverage|smith/.test(eq))score+=14;else score-=6}return{x,score}}).sort((a,b)=>b.score-a.score).slice(0,10)}
const parseDay321=line=>{const clean=String(line||'').replace(/^#+\s*/,'').trim();const m=clean.match(/^(montag|dienstag|mittwoch|donnerstag|freitag|samstag|sonntag|mo|di|mi|do|fr|sa|so|monday|tuesday|wednesday|thursday|friday|saturday|sunday)\b\s*(?:[-–—:|]\s*(.*))?$/i);if(m){const day=dayMap321[norm321(m[1]).replace(/\s/g,'')]||dayMap321[m[1].toLowerCase()];return{day,title:m[2]?.trim()||fullDay321[day]}}const t=clean.match(/^tag\s*(\d+)\s*(?:[-–—:|]\s*(.*))?$/i);if(t)return{day:'',title:t[2]?.trim()||`Training ${t[1]}`};if(/^(push|pull|beine|legs|oberkoerper|unterkoerper|ganzkoerper|upper|lower)(\s+[a-z0-9]+)?$/i.test(norm321(clean)))return{day:'',title:clean};return null}
const parseExercise321=line=>{let s=String(line||'').trim().replace(/^[•·▪◦*-]\s*/,'').replace(/^\d+[.)]\s*/,'').trim();if(!s)return null;const raw=s;let duration=null,sets=null,repsMin=null,repsMax=null,weight=null,rir=null;const dur=s.match(/(\d+(?:[.,]\d+)?)\s*(min(?:ute[n]?)?|mins?)\b/i);if(dur){duration=Math.max(1,Math.round(Number(dur[1].replace(',','.'))));s=s.replace(dur[0],' ')}const sr=s.match(/(\d+)\s*[x×]\s*(\d+)(?:\s*[-–]\s*(\d+))?/i)||s.match(/(\d+)\s*(?:saetze|sätze|sets?)\s*(?:x|×|a|à)?\s*(\d+)(?:\s*[-–]\s*(\d+))?/i);if(sr){sets=Math.max(1,Math.min(8,Number(sr[1])));repsMin=Math.max(1,Number(sr[2]));repsMax=sr[3]?Math.max(repsMin,Number(sr[3])):repsMin;s=s.replace(sr[0],' ')}else{const rr=s.match(/(\d+)(?:\s*[-–]\s*(\d+))?\s*(?:wdh\.?|wiederholungen|reps?)\b/i);if(rr){repsMin=Math.max(1,Number(rr[1]));repsMax=rr[2]?Math.max(repsMin,Number(rr[2])):repsMin;s=s.replace(rr[0],' ')}}
 const rirM=s.match(/\bRIR\s*[:=]?\s*(4\+|[0-4])\b/i);if(rirM){rir=rirM[1]==='4+'?4:Number(rirM[1]);s=s.replace(rirM[0],' ')}const kg=s.match(/([+]?[0-9]+(?:[.,][0-9]+)?)\s*kg(?:\s*\/\s*arm|\s*pro\s*arm|\s*je\s*arm|\s*gesamt)?/i);if(kg){weight=Number(kg[1].replace(',','.').replace('+',''));s=s.replace(kg[0],' ')}s=s.replace(/[(),;]+/g,' ').replace(/\s*[-–—|]\s*$/,'').replace(/\s{2,}/g,' ').trim();const name=s.replace(/\b(?:RIR|Gewicht|Dauer|Sätze|Saetze|Wdh)\b\s*[:=]?\s*$/i,'').trim();if(!name)return null;return{raw,rawName:name,duration,sets:sets||3,repsMin:repsMin||10,repsMax:repsMax||repsMin||10,weight,rir:rir??2}}
const parseText321=text=>{const lines=String(text||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean);let name='',sessions=[],current=null;for(const line of lines){const planLine=line.match(/^(?:plan|trainingsplan)\s*[:\-–]\s*(.+)$/i);if(planLine&&!sessions.length){name=planLine[1].trim();continue}const day=parseDay321(line);if(day){current={title:day.title,day:day.day,exercises:[]};sessions.push(current);continue}const ex=parseExercise321(line);if(!ex)continue;if(!current){if(!name&&sessions.length===0&&lines.indexOf(line)===0&&!/\d/.test(line)){name=line;continue}current={title:'Training 1',day:'',exercises:[]};sessions.push(current)}current.exercises.push(ex)}sessions=sessions.filter(s=>s.exercises.length);return{name:name||'Importierter Trainingsplan',sessions}}
const mappedExercise321=(parsed,match)=>{if(!match)return null;const x=match,cardio=isCardio321(x),image=x?.thumbUrl||x?.gifUrl||'';const base={name:pretty321(x?.name||parsed.rawName),query:x?.name||parsed.rawName,equipment:x?.equipment||'',image,gifUrl:x?.gifUrl||image,thumbUrl:x?.thumbUrl||image,muscle:x?.muscle||'',bodyPart:x?.bodyPart||'',category:x?.category||''};if(cardio)return{...base,kind:'cardio',sets:1,reps:'',duration:parsed.duration||20};const repsText=parsed.repsMax!==parsed.repsMin?`${parsed.repsMin}–${parsed.repsMax}`:String(parsed.repsMin),unilateral=/je seite|pro arm|je arm|einarm|einbein|single arm|single leg|one arm|one leg/i.test(`${parsed.raw} ${x?.name||''}`),body=/body.?weight|körpergewicht|koerpergewicht/.test(String(x?.equipment||'').toLowerCase());const plannedSets=Array.from({length:parsed.sets},()=>({reps:parsed.repsMin,weight:body?0:(parsed.weight||0),extraWeight:body?(parsed.weight||0):0,useExtra:body&&!!parsed.weight,rirTarget:parsed.rir}));return{...base,sets:parsed.sets,reps:repsText,unilateral,plannedSets}}
const refreshPlans321=()=>{const page=[...document.querySelectorAll('.page')].find(p=>p.querySelector('.page-head h1')?.textContent?.trim()==='Trainingspläne'),back=page?.querySelector('.sub-head button');if(back){back.click();setTimeout(()=>{const btn=[...document.querySelectorAll('.menu-list button')].find(b=>b.querySelector('strong')?.textContent?.trim()==='Trainingspläne');btn?.click()},80)}}
const openImport321=()=>{document.querySelector('.plan-import-backdrop-v321')?.remove();const ov=document.createElement('div');ov.className='plan-import-backdrop-v321';ov.innerHTML=`<section class="plan-import-v321"><header><div><small>PLAN-IMPORT</small><h2>Plan aus Text einfügen</h2><p>ChatGPT-Plan oder anderen Text einfügen. FitTogether erkennt Tage, Übungen, Sätze, Wiederholungen, Gewicht, RIR und Cardio-Zeit.</p></div><button type="button" data-close>×</button></header><div class="plan-import-body-v321"><textarea data-text placeholder="Beispiel:\nMontag – Push\nBankdrücken 3×10, 9 kg, RIR 2\nSchulterdrücken 3×12\n\nMittwoch – Cardio\nFahrrad 35 Minuten"></textarea><button type="button" class="plan-import-analyze-v321" data-analyze>Plan analysieren</button><div class="plan-import-result-v321"></div></div></section>`;document.body.appendChild(ov);const close=()=>ov.remove();ov.querySelector('[data-close]').onclick=close;ov.addEventListener('click',e=>{if(e.target===ov)close()});ov.querySelector('[data-analyze]').onclick=async()=>{const text=ov.querySelector('[data-text]').value.trim(),result=ov.querySelector('.plan-import-result-v321');if(!text){result.innerHTML='<p class="plan-import-error-v321">Bitte zuerst einen Trainingsplan einfügen.</p>';return}result.innerHTML='<div class="plan-import-loading-v321">Übungsdatenbank wird abgeglichen …</div>';const parsed=parseText321(text);if(!parsed.sessions.length){result.innerHTML='<p class="plan-import-error-v321">Ich konnte keine Trainingstage mit Übungen erkennen. Schreibe z. B. „Montag – Push“ und darunter „Bankdrücken 3×10“.</p>';return}const db=await loadDb321();if(!db.length){result.innerHTML='<p class="plan-import-error-v321">Die Übungsdatenbank konnte gerade nicht geladen werden. Bitte später erneut versuchen.</p>';return}const state={name:parsed.name,sessions:parsed.sessions.map(s=>({...s,exercises:s.exercises.map(e=>{const ranked=rank321(e.rawName,db),best=ranked[0];return{parsed:e,ranked,match:best&&best.score>=24?best.x:null,score:best?.score||0}})}))};const draw=()=>{const rows=state.sessions.flatMap((s,si)=>s.exercises.map((e,ei)=>({s,si,e,ei}))),unresolved=rows.filter(r=>!r.e.match).length,review=rows.filter(r=>r.e.match&&r.e.score<70).length;result.innerHTML=`<div class="plan-import-preview-head-v321"><label><span>Planname</span><input data-name value="${esc321(state.name)}"></label><div><b>${state.sessions.length} Tage</b><span>${rows.length} Übungen</span></div></div>${state.sessions.map((s,si)=>`<section class="plan-import-day-v321"><div class="plan-import-day-head-v321"><span><small>${s.day?esc321(fullDay321[s.day]||s.day):`TAG ${si+1}`}</small><strong>${esc321(s.title)}</strong></span><b>${s.exercises.length}</b></div>${s.exercises.map((entry,ei)=>{const m=entry.match,img=m&&(m.thumbUrl||m.gifUrl),status=!m?'Nicht erkannt':entry.score>=70?'Erkannt':'Bitte prüfen',cls=!m?'bad':entry.score>=70?'good':'review';return`<div class="plan-import-ex-v321 ${cls}">${img?`<img src="${esc321(img)}" loading="lazy" alt="">`:'<div class="plan-import-noimg-v321">?</div>'}<div class="plan-import-ex-copy-v321"><strong>${esc321(entry.parsed.rawName)}</strong><small>${entry.parsed.duration?`${entry.parsed.duration} Min.`:`${entry.parsed.sets}×${entry.parsed.repsMin}${entry.parsed.repsMax!==entry.parsed.repsMin?`–${entry.parsed.repsMax}`:''} · RIR ${entry.parsed.rir}${entry.parsed.weight?` · ${entry.parsed.weight} kg`:''}`}</small><select data-select="${si}:${ei}"><option value="">Übung auswählen …</option>${entry.ranked.map((r,ri)=>`<option value="${ri}" ${m===r.x?'selected':''}>${esc321(pretty321(r.x.name))} · ${esc321(r.x.equipment||'')}</option>`).join('')}</select></div><em>${status}</em></div>`}).join('')}</section>`).join('')}<div class="plan-import-summary-v321"><span>${unresolved?`${unresolved} nicht erkannt`:review?`${review} Zuordnung${review===1?'':'en'} bitte prüfen`:'Alle Übungen erkannt'}</span><button type="button" data-save ${unresolved?'disabled':''}>Plan speichern</button></div>`;result.querySelector('[data-name]').oninput=e=>state.name=e.target.value;result.querySelectorAll('[data-select]').forEach(sel=>sel.onchange=()=>{const[si,ei]=sel.dataset.select.split(':').map(Number),entry=state.sessions[si].exercises[ei],idx=sel.value===''?-1:Number(sel.value);entry.match=idx>=0?entry.ranked[idx]?.x:null;entry.score=idx>=0?entry.ranked[idx]?.score||0:0;draw()});result.querySelector('[data-save]')?.addEventListener('click',()=>{const sessions=state.sessions.map((s,si)=>({title:s.day?(s.title&&norm321(s.title)!==norm321(fullDay321[s.day])?`${fullDay321[s.day]} · ${s.title}`:fullDay321[s.day]):s.title||`Training ${si+1}`,day:s.day||undefined,exercises:s.exercises.map(e=>mappedExercise321(e.parsed,e.match)).filter(Boolean)}));const weekdays=sessions.map(s=>s.day||null),plans=read321('ft-plans',[]),plan={name:state.name.trim()||'Importierter Trainingsplan',days:sessions.length,minutes:60,sessions,weekdays,createdAt:Date.now(),imported:true,source:'Textimport'};write321('ft-plans',[...plans,plan]);try{window.FitTogetherCloud?.upload?.()}catch{}result.innerHTML='<div class="plan-import-success-v321">✓ Plan gespeichert und mit der FitTogether-Übungsbibliothek verknüpft.</div>';setTimeout(()=>{close();refreshPlans321()},450)})};draw()}}}
const enhance321=()=>{if(document.querySelector('.active-training,.rest-overlay,.training-overlay,.plan-import-backdrop-v321'))return;const page=[...document.querySelectorAll('.page')].find(p=>p.querySelector('.page-head h1')?.textContent?.trim()==='Trainingspläne');if(!page||page.querySelector('.plan-import-launch-v321'))return;const b=document.createElement('button');b.type='button';b.className='plan-import-launch-v321';b.innerHTML='<span><small>PLAN-IMPORT</small><strong>Plan aus Text importieren</strong><em>ChatGPT-Plan einfügen und Übungen automatisch zuordnen</em></span><b>Importieren</b>';b.onclick=openImport321;const hint=page.querySelector('.plan-editor-hint-v301'),head=page.querySelector('.page-head');(hint||head)?.insertAdjacentElement('afterend',b)}
let q321=false;const schedule321=()=>{if(q321)return;q321=true;requestAnimationFrame(()=>{q321=false;enhance321()})}
if(typeof document!=='undefined'){const start=()=>{enhance321();new MutationObserver(m=>{if(m.some(x=>x.addedNodes.length||x.removedNodes.length))schedule321()}).observe(document.body,{childList:true,subtree:true});window.addEventListener('pageshow',schedule321)};document.body?start():document.addEventListener('DOMContentLoaded',start,{once:true})}
